import React from "react";

const Title = ({ title }) => {
  return (
    <div className="mt-2">
      <h1 className="text-left text-xl ml-4 text-blue-500">{title}</h1>
    </div>
  );
};

export default Title;
